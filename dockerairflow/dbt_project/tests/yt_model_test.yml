version: 2

models:
  - name: yt_model
    columns:
      - name: GENRE
        tests:
          - not_null
          - type: string
      - name: total
        tests:
          - not_null
          - type: integer
    tests:
      - unique_combination:
          column_pairs: [[GENRE]]
